enum AppAlertType { fallback, info, warning, danger, success }

enum AppAlertSize { xSmall, small, medium, large, xLarge }

enum AppAlertPlacement {
  top,
  topLeft,
  topRight,
  center,
  bottom,
  bottomLeft,
  bottomRight,
}
